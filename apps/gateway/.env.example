# ClaudeChatAPP Gateway — 可拆卸的模型/厂商配置
#
# 复制本文件为 `.env`（位于 apps/gateway/ 目录下，已 gitignore）。
# Gateway 启动时会自动加载 `.env`，因此「在哪里跑，就配哪里的厂商凭据」。
#
# 换模型/厂商 = 改下面三个 ANTHROPIC_* 变量。所有 Anthropic 兼容端点都通用：
#   DeepSeek        https://api.deepseek.com/anthropic
#   Kimi (Moonshot) https://api.moonshot.cn/anthropic
#   GLM (智谱)      https://open.bigmodel.cn/api/anthropic
#   通义千问         （提供 Anthropic 兼容端点时填对应地址）
#
# 不填则回退到本机 `claude login` 登录态（第一方 Anthropic）。

# 厂商兼容端点
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic

# 厂商 API Key
ANTHROPIC_AUTH_TOKEN=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 模型名（换成目标厂商提供的模型 ID）
ANTHROPIC_MODEL=deepseek-v4-flash

# 可选：Gateway 自身的鉴权 key（手机端连接时需要）
# GATEWAY_API_KEY=your-6-digit-key
